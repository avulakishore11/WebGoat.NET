trigger:
- master

pool:
 name: 'buildagent-payment'

stages:
  - stage:
    jobs:
    - job: 
      steps:
      - task: Docker@2
        inputs:
          containerRegistry: 'acr-svc'
          repository: 'payment-repository'
          command: 'buildAndPush'
          Dockerfile: '**/Dockerfile'